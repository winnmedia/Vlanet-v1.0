# 개발 로그 - 2025년 1월 24일

## 🔥 긴급 이슈 대응

### 배포 후 발생한 치명적 문제들
1. **영상 기획 페이지 404 에러**
2. **프로젝트 관리 페이지 무한 리퀘스트로 무한 새로고침**
3. **영상 피드백 인터페이스 디자인 완전 붕괴**

## 📋 작업 내용

### 1. UI/UX 개선 작업 (오전)

#### 1.1 홈 화면 시계/날짜 크기 수정
- **문제**: 백업 폴더의 원본 디자인과 다른 크기로 표시됨
- **해결**: 
  - `CmsHomeFix.scss` 파일 생성
  - 시계 크기를 원본과 동일한 120px로 복원
  - 날짜 표시 스타일링 수정

#### 1.2 로딩 애니메이션 개선
- **문제**: 글자가 뱅글뱅글 도는 애니메이션, 중복 로딩 오버레이
- **해결**:
  - `LoadingAnimationFix.scss` 파일 생성
  - 회전 애니메이션을 프로그레스 바로 변경
  - 라우트 변경 시 상단 로딩바 추가
  - 중복 로딩 방지 로직 구현

#### 1.3 백엔드 데이터 로딩 문제
- **문제**: 피드백 페이지에서 프로젝트 ID를 인식하지 못함
- **해결**:
  - Next.js router.query 사용으로 변경
  - API 요청/응답 디버깅 로그 추가
  - axios 인터셉터에 상세 로깅 추가

### 2. Vercel 배포 문제 해결

#### 2.1 배포 오류 수정
- **문제 1**: `vercel.json` 스키마 검증 실패
  - `github.autoAlias`가 배열로 설정됨 (boolean이어야 함)
  - 해결: 해당 필드 제거

- **문제 2**: GitHub Actions Vercel 배포 실패
  - amondnet/vercel-action 오류
  - 해결: 공식 Vercel CLI를 사용하는 워크플로우로 변경

- **문제 3**: VideoPlanning.jsx 빌드 오류
  - "Unterminated regexp literal" 오류
  - SVG path 속성의 불필요한 중괄호 제거
  - 파일이 너무 커서 Next.js 15.4.2 SWC 파서가 처리 못함

### 3. 긴급 핫픽스 (오후)

#### 3.1 프로젝트 관리 페이지 무한 루프 수정
```javascript
// 수정 전
useEffect(() => {
  loadProjectData()
}, [dispatch, project_list, user]) // 문제: 데이터 업데이트 시 재실행

// 수정 후
useEffect(() => {
  loadProjectData()
}, [dispatch]) // 의존성에서 project_list와 user 제거
```

#### 3.2 피드백 페이지 디자인 복구
- 누락된 CSS 파일들 때문에 디자인 붕괴
- `FeedbackPage.scss` 생성하여 기본 레이아웃 복구
- 비디오 플레이어, 피드백 리스트, 입력 영역 스타일링

#### 3.3 VideoPlanning 페이지 임시 조치
- 계속되는 빌드 오류로 페이지 임시 비활성화
- `videoplanning.js` → `videoplanning.js.disabled`
- 리팩토링 필요 (파일을 작은 컴포넌트로 분할)

## 🚀 배포 이력

### 1차 배포 (10:56)
- **결과**: 실패
- **원인**: `vercel.json` 스키마 오류

### 2차 배포 (10:58)
- **결과**: 실패
- **원인**: VideoPlanning.jsx 구문 오류

### 3차 배포 (11:10)
- **결과**: 성공
- **조치**: VideoPlanning 페이지 임시 비활성화

### 4차 배포 (긴급 핫픽스)
- **결과**: 진행 중
- **수정사항**: 
  - 무한 리퀘스트 버그 수정
  - 피드백 페이지 CSS 복구

## 📊 현재 상태

### ✅ 완료된 작업
1. 홈 화면 시계/날짜 크기 복원
2. 로딩 애니메이션 프로그레스 바로 변경
3. 프로젝트 관리 페이지 무한 루프 해결
4. 피드백 페이지 기본 디자인 복구
5. Vercel 배포 프로세스 안정화

### ⚠️ 진행 중인 이슈
1. **VideoPlanning 페이지 복구**
   - 파일 크기: 3000+ 줄
   - 해결 방향: 컴포넌트 분할 리팩토링
   - 예상 작업량: 4-6시간

### 🔧 기술적 발견사항
1. **Next.js 15.4.2 SWC 파서 한계**
   - 대용량 JSX 파일 처리 시 오류 발생
   - 2000줄 이상의 컴포넌트는 분할 권장

2. **React useEffect 의존성 관리**
   - Redux 상태를 의존성에 포함 시 무한 루프 위험
   - 초기 로드만 필요한 경우 의존성 최소화

3. **Vercel 배포 최적화**
   - 공식 CLI 사용이 더 안정적
   - 캐시 활용으로 빌드 시간 단축

## 🎯 다음 작업 계획

### 즉시 필요한 작업
1. VideoPlanning.jsx 리팩토링
   - StorySection, SceneSection, ShotSection으로 분할
   - 각 섹션을 독립적인 컴포넌트로 분리

2. 피드백 페이지 디자인 완성
   - 원본 디자인과 비교하여 세부 스타일 조정
   - 반응형 디자인 개선

### 중기 개선사항
1. 전체 CSS 구조 정리
   - 중복된 스타일 제거
   - 컴포넌트별 모듈화

2. 성능 최적화
   - 불필요한 리렌더링 방지
   - API 호출 최적화

## 💡 교훈
1. **대규모 컴포넌트는 항상 분할하여 관리**
2. **useEffect 의존성은 신중하게 설정**
3. **CSS 파일 임포트는 실제 파일 존재 여부 확인 필수**
4. **긴급 배포 시에도 로컬 빌드 테스트는 필수**

---
*작성자: Claude Assistant*  
*프로젝트: VideoPlanet v1.0*  
*환경: Next.js 15.4.2, React 18.3.1*