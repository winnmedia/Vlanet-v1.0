{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'RAILWAY_ENVIRONMENT=production' >> .env"
  },
  "deploy": {
    "startCommand": "export DJANGO_SETTINGS_MODULE=config.settings.railway && export RAILWAY_ENVIRONMENT=production && python manage.py migrate --settings=config.settings.railway && python manage.py collectstatic --noinput --settings=config.settings.railway && mkdir -p /app/media/feedback_file && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --timeout 300 --workers 3",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "variables": {
    "DJANGO_SETTINGS_MODULE": "config.settings.railway",
    "RAILWAY_ENVIRONMENT": "production"
  },
  "volumes": [
    {
      "mount": "/app/media",
      "name": "media-storage"
    }
  ]
}