{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --settings=config.settings_minimal && python manage.py collectstatic --noinput --settings=config.settings_minimal && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --timeout 120 --workers 1",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  },
  "volumes": [
    {
      "mount": "/app/media",
      "name": "media-storage"
    }
  ]
}