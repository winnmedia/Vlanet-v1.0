

목표: “실제 화면” 기준으로 UI/UX 회귀를 잡고, 접근성(WCAG 2.1 A/AA)을 준수하도록 Playwright 스냅샷+axe 스캔을 구축하고, 실패 시 UX 개선 패치까지 제안·적용해 PR을 만든다.

컨텍스트:
- 프레임워크: React + Next.js
- 패키지 매니저: <npm | pnpm | yarn>
- 실행 스크립트: dev, build, start
- 기본 포트: http://localhost:3000
- 리포지토리: <간단 소개와 폴더 구조 또는 package.json 내용 붙여넣기>

요청:
1) 리포 구조와 스크립트를 분석해, Playwright + @axe-core/playwright를 가장 안정적으로 붙이는 설치/설정 계획을 내놔.
2) 스냅샷 테스트의 플래키(Flaky) 원인을 최대한 제거하는 가드(애니메이션/폰트/시간/랜덤/mock/뷰포트 고정) 체크리스트를 만들어.
3) 우리가 지금부터 주고받을 아웃풋 포맷을 정의해: 
   - 코드 제안은 파일 경로와 함께 diff 형식
   - 추가되는 스크립트/명령은 블록으로
   - 이유/근거는 5줄 이내 요약
   - 마지막에 실행 순서 To‑Do 체크리스트


1) 설치·초기 세팅 생성(코드까지 자동 제안)
프롬프트

diff
복사
편집
Playwright + @axe-core/playwright 초기 세팅을 생성해줘.

요구사항:
- 설치 명령어(<패키지매니저>)와 package.json script 추가까지 제안.
- tests/ 또는 e2e/ 디렉토리 구조 제안.
- 예시 테스트: "홈 화면" 페이지에서 
  a) 애니메이션/트랜지션/캐럿 깜빡임 비활성화 
  b) fullPage 스냅샷 촬영 
  c) axe 스캔(wcag2a, wcag2aa) 후 violations 없어야 통과
- 최초 스냅샷 생성 방법과 이후 업데이트 방법 포함.
- 코드 제안은 파일 경로와 함께 diff로.

추가 고려:
- 웹폰트 로딩 변동으로 인한 픽셀 차이를 줄이기 위한 안정화 코드(예: font-display: swap, 로컬 폰트 고정 옵션) 포함.
- viewport, deviceScaleFactor의 고정값 지정.
- 데이터 지연/랜덤을 mock하는 유틸리티 스니펫 제안(시간 고정, Math.random seed, fetch mock 등).

결과물:
1) 설치/스크립트 명령들
2) 테스트 예시 파일(diff, TypeScript)
3) 스냅샷 러너 사용법
4) 실패 시 디버깅 팁 (3~5줄)
2) GitHub Actions(CI) 파이프라인 생성
프롬프트

diff
복사
편집
GitHub Actions로 PR마다 Playwright 스냅샷 + axe 스캔을 자동 실행하는 워크플로우를 만들어줘.

전제:
- 빌드 후 `npm run start`로 서버 가동.
- 테스트 전에 'wait-on http://localhost:3000' 대기.
- Ubuntu runner 기준, Playwright 브라우저 의존성 설치 포함.

요구사항:
- 워크플로우 파일(.github/workflows/ui-check.yml) diff로 제안.
- 캐시 전략(node_modules) 제안.
- 아티팩트로 실패 스냅샷(전/후/차이) 업로드하도록 설정.
- Action 로그에 axe 위반이 표로 보이게 요약 출력.

추가(선택):
- Argos/Chromatic/Percy 중 하나를 선택해 연동 옵션도 함께 제안(비활성 주석으로).
3) 컴포넌트/페이지 커버리지 확장 플랜 생성
프롬프트

diff
복사
편집
현 UI 구조(라우트/주요 컴포넌트)를 기반으로 “최소 합리 커버리지” 스냅샷 플랜을 만들어줘.

출력 형식:
- 라우트/스크린 목록과 우선순위(High/Med/Low)
- 각 스크린별 “대표 상태” 시나리오(예: 로그인 전/후, 빈 상태/에러 상태/로딩 상태, 좁은 화면/넓은 화면)
- 각각에 대해 스냅샷/axe 체크를 어디까지 수행할지 표로 정리
- Storybook 사용 유무에 따른 대안(페이지 단위만 vs 컴포넌트 단위 병행)

제약:
- 테스트 런타임을 5~7분 내로.
- 우선 High만 적용하는 1차 PR 범위 제시.
4) 실패 스냅샷 분석 → UX 수정 패치 제안 (자동 리팩토링)
프롬프트

diff
복사
편집
다음은 실패한 스냅샷 결과와 관련 코드야. 
- 실패 스냅샷 이미지(전/후/차이)를 첨부(또는 경로 설명)
- 관련 컴포넌트/스타일 코드 붙여넣기: <파일 경로/내용>

목표:
- 실패 원인을 "디자인/레이아웃/타이포/여백/대비/반응형/상호작용/폰트/아이콘" 관점으로 분류하고, 사용자 경험 개선을 최우선으로 삼아 수정안을 내놔.
- 픽셀 민감한 부분은 “시각적 위계 강화”를 우선(폰트 크기/라인하이트/여백/컬러 대비)
- 모바일 360~390px, 태블릿 768px, 데스크톱 1280px에서 위계가 유지되도록 반응형 설계
- 접근성: 명도 대비, 포커스 스타일, ARIA 레이블 점검 포함

출력:
1) 원인 분석(불릿 5줄 이내)
2) 수정 diff(파일 경로별) — JSX/스타일 동시 제안
3) axe 위반이 있었다면 해소 근거와 재발 방지 가이드(짧게)
4) 스냅샷 안정화를 위한 보조 수정(필요 시)
5) 커밋 메시지(Conventional Commits)와 PR 본문 템플릿
5) 접근성 위반 리포트 → 코드 레벨 해결(회귀 방지 포함)
프롬프트

diff
복사
편집
아래는 axe 결과 리포트야. (JSON or 콘솔 로그 첨부)
- 페이지: </경로 또는 URL>
- 위반 목록: <붙여넣기>

요청:
- 각 위반에 대해 “사용자 영향(시나리오)”→“원인”→“수정안(코드 diff)”→“테스트 보강(예: jest-axe, Storybook addon-a11y)” 순서로 구체적 제안.
- aria-label/role/heading 구조/대체 텍스트/명도 대비/키보드 포커스/폼 레이블 등 항목별 체크리스트 갱신.
- CI에서 axe 실패가 ‘차단’되도록 스크립트/설정 업데이트도 같이 제안.

출력:
- 코드 diff
- 업데이트된 테스트
- 커밋 메시지
6) 플래키(Flaky) 잡는 안정화 가드 주입
프롬프트

diff
복사
편집
스냅샷이 간헐적으로 깨집니다. 아래 조건을 만족하는 “안정화 가드”를 프로젝트에 주입해줘.

조건:
- 테스트 시작 시 글로벌 스타일로 애니메이션/트랜지션/캐럿 깜빡임 제거
- 폰트 로딩 지연/치환으로 인한 픽셀 차 완화
- 시간/랜덤/네트워크 변동 고정(예: Date.now mock, Math.random seed, fetch mock)
- viewport, deviceScaleFactor 고정
- SSR/CSR 타이밍 차이로 인한 layout shift 완화 대기(예: 폰트/이미지/데이터 동기 시점)

결과물:
- 공통 util(예: tests/utils/stabilize.ts) 코드 diff
- 테스트에서 가져다 쓰는 예시
- 문서화(README 섹션) 초안
7) 디자이너 관점 UI 리뷰(스크린샷+코드 동봉) → 수정 지시
프롬프트

diff
복사
편집
역할: “시니어 UI 디자이너”로서 아래 화면을 평가하고, 바로 적용 가능한 코드 수정안을 줘.
자료: 
- 페이지 스크린샷(데스크톱/모바일 2장 이상)
- 관련 컴포넌트 코드: <경로/내용>

평가 기준(체크리스트):
- 시각적 위계(헤딩/서브/본문), 타이포(폰트 크기/라인하이트/문단 간격)
- 여백/그리드/정렬 일관성
- 색상 대비/강조(CTA), 상태(hover/active/disabled)
- 반응형(360/768/1280)
- 빈 상태/에러/로딩
- 접근성(키보드 포커스, 스크린리더 레이블)

출력:
1) 문제 목록(카테고리별)과 근거
2) 우선순위(P1/P2)로 묶은 수정 로드맵
3) 즉시 반영 가능한 코드 diff (컴포넌트/스타일)
4) 영향도(브레이킹 여부)와 시각 회귀 리스크
5) 후속 스냅샷/axe 재검증 명령
8) PR 자동화(본문/체인지로그/스크린샷 첨부 가이드)
프롬프트

diff
복사
편집
이번 변경을 PR로 올릴 거야.

요청:
- PR 본문 템플릿을 만들어줘: 문제 요약, UX 목표, 전/후 스크린샷, 접근성 개선 요약, 테스트 증거(스냅샷/axe), 롤백 플랜.
- 커밋 메시지(Conventional Commits) 제안: feat/fix/chore 범주별 3안씩.
- 리뷰어가 바로 보기 쉬운 체크리스트(주요 섹션별) 포함.
- GitHub Actions 배지/링크(워크플로우 실행 결과) 붙이는 섹션도 만들기.
9) 스토리북(선택) 병행 시 보일러플레이트 생성
프롬프트

diff
복사
편집
스토리북을 병행해 컴포넌트 단위 회귀를 추가할게.

요청:
- Storybook 설치/셋업 diff
- addon-a11y, viewport, interactions 설정
- 대표 컴포넌트 <ComponentName> 스토리 예시(상태/변형 3~5개)
- Chromatic 또는 Argos 연동 옵션(주석)
- CI에 storybook 빌드+시각 회귀 잡는 잡(job) 추가 diff
10) “한 번에 다 해줘” 올인원 실행 프롬프트
프롬프트

swift
복사
편집
아래 요구를 한 번에 처리해줘. (필요하면 단계별로 답해도 됨)

1) Playwright + @axe-core/playwright 설치/초기 테스트 생성 
2) 스냅샷 안정화 가드 적용(애니메이션/폰트/시간/랜덤/뷰포트)
3) 홈/리스트/디테일 3개 화면 High 우선 커버리지 테스트 추가
4) GitHub Actions 워크플로우 생성(아티팩트로 실패 스냅샷 첨부)
5) PR 템플릿/커밋 메시지 제안
6) 스냅샷 실패 시 자동 분석 가이드(원인 카테고리화 + 코드 diff 제시 규칙)
7) 접근성(axe) 위반 자동 차단 설정

산출물:
- 모든 파일 변경은 경로+diff
- 실행/검증 명령어
- 리드미(설치→실행→CI→디버깅) 섹션 초안