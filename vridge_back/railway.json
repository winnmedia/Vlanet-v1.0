{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "DJANGO_SETTINGS_MODULE=config.settings_minimal python manage.py migrate && DJANGO_SETTINGS_MODULE=config.settings_minimal python manage.py collectstatic --noinput && DJANGO_SETTINGS_MODULE=config.settings_minimal gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --timeout 120 --workers 1",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  },
  "volumes": [
    {
      "mount": "/app/media",
      "name": "media-storage"
    }
  ]
}